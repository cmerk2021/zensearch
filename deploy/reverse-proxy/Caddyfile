# Caddy reverse proxy for Zen — automatic HTTPS.
# Set ZEN_BASE_URL=https://zen.example.com and ZEN_TRUSTED_PROXIES to the
# Caddy host/container IP.

zen.example.com {
	encode zstd gzip
	request_body {
		max_size 64MB
	}
	reverse_proxy 127.0.0.1:3000 {
		header_up X-Real-IP {remote_host}
	}
}
